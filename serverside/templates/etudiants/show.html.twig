{% extends 'base.html.twig' %}

{% block title %}Etudiants{% endblock %}

{% block body %}
    <h1>Etudiants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>Nom_etudiants</th>
                <td>{{ etudiant.nomEtudiants }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ etudiant.password }}</td>
            </tr>
            <tr>
                <th>Prenom_etudiants</th>
                <td>{{ etudiant.prenomEtudiants }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ etudiant.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etudiants_index') }}">back to list</a>

    <a href="{{ path('app_etudiants_edit', {'id': etudiant.id}) }}">edit</a>

    {{ include('etudiants/_delete_form.html.twig') }}
{% endblock %}
