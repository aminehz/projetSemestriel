{% extends 'base.html.twig' %}

{% block title %}Stages{% endblock %}

{% block body %}
    <h1>Stages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stage.id }}</td>
            </tr>
            <tr>
                <th>Titre_stage</th>
                <td>{{ stage.titreStage }}</td>
            </tr>
            <tr>
                <th>Description_stage</th>
                <td>{{ stage.descriptionStage }}</td>
            </tr>
            <tr>
                <th>Image_stage</th>
                <td>{{ stage.imageStage }}</td>
            </tr>
            <tr>
                <th>Nom_poste</th>
                <td>{{ stage.nomPoste }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stages_index') }}">back to list</a>

    <a href="{{ path('app_stages_edit', {'id': stage.id}) }}">edit</a>

    {{ include('stages/_delete_form.html.twig') }}
{% endblock %}
